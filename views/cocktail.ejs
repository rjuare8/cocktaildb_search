<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= cocktail.name %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #333; /* Darker background color */
            color: #f4f4f4; /* Lighter text color for contrast */
            margin: 0;
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: #f4f4f4; /* Light color for the heading */
            margin-bottom: 20px;
        }
        .cocktail-detail {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            max-width: 1200px; /* Increase max-width for larger images */
            margin: 0 auto;
        }
        .cocktail-image {
            flex: 1;
            margin-right: 20px;
        }
        .cocktail-image img {
            max-width: 400px; /* Larger size for the cocktail image */
            border-radius: 8px;
            display: block;
            margin: 0 auto;
        }
        .cocktail-info {
            flex: 2;
        }
        .cocktail-info p {
            font-size: 18px;
            margin: 10px 0;
        }
        .ingredients {
            margin-top: 20px;
        }
        .ingredient-item {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }
        .ingredient-item img {
            width: 70px; /* Larger size for ingredient images */
            height: 70px;
            margin-right: 10px;
            border-radius: 4px;
        }
        .ingredient-item p {
            margin: 0;
        }
        .back-buttons {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
        }
        .back-button {
            display: block;
            padding: 10px 20px;
            background-color: #444; /* Dark background color for button */
            color: #fff; /* White text color */
            border: none;
            border-radius: 4px;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
        }
        .back-button:hover {
            background-color: #555; /* Slightly lighter on hover */
        }
    </style>
</head>
<body>

    <h1><%= cocktail.name %></h1>
    
    <div class="cocktail-detail">
        <div class="cocktail-image">
            <img src="<%= cocktail.icon %>" alt="<%= cocktail.name %>">
        </div>
        <div class="cocktail-info">
            <p><strong>Instructions:</strong> <%= cocktail.instructions %></p>
            <div class="ingredients">
                <h3>Ingredients:</h3>
                <% ingredients.forEach(function(ingredient) { %>
                    <div class="ingredient-item">
                        <img src="https://<%= ingredient.icon %>" alt="<%= ingredient.ingredient %>">
                        <p><strong><%= ingredient.ingredient %></strong> - <%= ingredient.measure || 'to taste' %></p>
                    </div>
                <% }); %>
            </div>
        </div>
    </div>

    <div class="back-buttons">
        <a href="/" class="back-button">Back to List</a>
    </div>

</body>
</html>
